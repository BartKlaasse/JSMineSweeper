const Header = () => {
  const template = `
    <header>
    <h1> My App</h1>
    <p>this is a boilerplate for a simple js workflow using parcel</p>
    </header>
    `;

  return template;
};

export default Header;
